import React, { useState, useEffect } from 'react'

function Board({boardSize, dificullity}) {
    const mines = Math.floor(boardSize*boardSize*0.15)
    return (
        <div>
            
        </div>
    )
}

export default Board